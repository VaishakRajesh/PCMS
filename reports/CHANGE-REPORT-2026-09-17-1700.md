# Change Report

- **Date**: 2026-09-17 (UTC; wall-clock time unavailable — shell/timestamp tools unreachable, afternoon file write)
- **Agent**: swe
- **Objective**: Fix `isTemplate is not a function` crash on dashboard theme save.
- **Status**: Completed

## Summary

Root cause: `src/app/dashboard/actions.ts` imported `isTemplate` from
`src/lib/themes.ts`, but that function was never defined there (authoring
oversight) — so saving the theme crashed at runtime. Added the missing
type-guard export; no other files needed changes.

## Files Changed
| File | Type | Description |
|------|------|-------------|
| src/lib/themes.ts | Modified | Added `isTemplate(id)` type guard (8 lines incl. comments) |

## Details
### src/lib/themes.ts
- `isTemplate()` narrows any string to `TemplateId` via registry key check.
- Verified all cross-file imports resolve: `TEMPLATES`, `TemplateId`,
  `DEFAULT_TEMPLATE`, `availableTemplates`, `isTemplate`, `resolveTemplate`
  are all defined and match every usage in `actions.ts`, `[slug]/page.tsx`,
  and `design/page.tsx`.

## Validation Performed
- Re-read full `themes.ts` → export present, signatures consistent.
- Runtime/build check pending on user machine: open
  `/dashboard/design`, hit Save theme → should save + revalidate with no
  error; `npm run build` should stay green.

## Known Issues / Follow-ups
- None new. Phase 3 templates + manual Python deletion still open.

## Rollback Notes
- To revert: delete lines 23–27 (`isTemplate`) from `src/lib/themes.ts`.
  (Reverting reintroduces the crash — not advised.)
